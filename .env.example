# Database
DATABASE_URL=postgres://velvetscoop:velvetscoop@localhost:5433/velvetscoop

# Cache / queues
REDIS_URL=redis://localhost:6380

# Server
PORT=3000
NODE_ENV=development
DOMAIN=localhost

# Secrets (generate with `openssl rand -base64 48`)
SESSION_SECRET=changeme
COOKIE_SECRET=changeme
CSRF_SECRET=changeme

# Authentik OIDC (Phase 3 - placeholders for now)
AUTHENTIK_ISSUER_URL=https://auth.example.com/application/o/velvet-scoop/
AUTHENTIK_CLIENT_ID=
AUTHENTIK_CLIENT_SECRET=
AUTHENTIK_REDIRECT_URI=http://localhost:3000/auth/callback

# Legacy (will be removed after Phase 2)
MONGO_URI=
JWT_SECRET=
