{
  "name": "@velvet-scoop/backend",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "dotenv -e ../../.env -- tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/apps/backend/src/server.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:db:setup": "node --input-type=module -e \"import { execSync } from 'node:child_process'; const URL = 'postgres://velvetscoop:velvetscoop@localhost:5433/velvetscoop_test'; try { execSync('docker exec website-v2-postgres-1 psql -U velvetscoop -d velvetscoop -c \\\"CREATE DATABASE velvetscoop_test\\\"', { stdio: 'inherit' }); } catch {} execSync('npx prisma migrate deploy', { stdio: 'inherit', env: { ...process.env, DATABASE_URL: URL } });\"",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "prisma": "dotenv -e ../../.env -- prisma"
  },
  "prisma": {
    "schema": "prisma/schema.prisma"
  },
  "dependencies": {
    "@fastify/cookie": "^11.0.1",
    "@fastify/cors": "^10.0.1",
    "@fastify/rate-limit": "^10.2.1",
    "@prisma/client": "^5.22.0",
    "@velvet-scoop/shared": "workspace:*",
    "fastify": "^5.1.0",
    "ioredis": "^5.4.1",
    "openid-client": "^5.7.0"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "dotenv-cli": "^7.4.2",
    "prisma": "^5.22.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "vitest": "^2.1.5"
  }
}
